export async function predictFloodProbability(_features: unknown): Promise<number | undefined> {
  // TODO: call Hugging Face Inference API when HF_TOKEN and HF_MODEL_ID are configured.
  return undefined;
}
